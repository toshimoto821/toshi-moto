name: PR Merge

on:
  pull_request:
    types:
      - closed
    branches:
      - main

jobs:
  pull-request-merge:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
          submodules: "recursive"

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "18"

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: npx nx affected --target=lint --base=origin/main --head=$GITHUB_SHA

      - name: Test
        run: npx nx affected --target=test --base=origin/main --head=$GITHUB_SHA

      - name: Build
        run: |
          npx nx affected --target=build

      - name: Determine the base commit
        id: determine_base
        run: echo "BASE=$(git merge-base origin/main HEAD~1)" >> $GITHUB_ENV

      - name: Release
        env:
          BASE: ${{ env.BASE }}
        run: |
          npx nx affected --target=version --base=$BASE --head=$GITHUB_SHA

      - name: GitHub Push
        uses: ad-m/github-push-action@v0.8.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          tags: true
